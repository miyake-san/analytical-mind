"""Reusable project code lives here.

Put functions and classes you want to import (and test) in this package, and
keep exploratory, one-off work in the `notebooks/` folder instead.

Example:

    def load_data(path):
        ...
"""
